# {{ persona.primary_name }}

> {{ get_string("projector.profile_desc").strip("> ") }}

## {{ get_string("projector.base_info") }}

| {{ get_string("projector.field") }} | {{ get_string("projector.value") }} |
|------|----|
| {{ get_string("projector.uid") }} | `{{ persona.uid }}` |
| {{ get_string("projector.confidence") }} | {{ (persona.confidence * 100) | int }}% |
| {{ get_string("projector.created_at") }} | {{ persona.created_at | format_time }} |
| {{ get_string("projector.last_active") }} | {{ persona.last_active_at | format_time }} |

## {{ get_string("projector.bindings") }}

{% if persona.bound_identities %}
| {{ get_string("projector.platform") }} | {{ get_string("projector.physical_id") }} |
|------|---------|
{% for platform, pid in persona.bound_identities %}
| {{ platform }} | `{{ pid }}` |
{% endfor %}
{% else %}
{{ get_string("projector.no_bindings") }}
{% endif %}

## {{ get_string("projector.personal_attrs") }}

{% if persona.persona_attrs %}
{% for key, value in persona.persona_attrs.items() %}
- **{{ key }}**: {{ value }}
{% endfor %}
{% else %}
{{ get_string("projector.no_attrs") }}
{% endif %}

## {{ get_string("projector.recent_events", count=(events | length)) }}

{% if events %}
{% for event in events %}
- `{{ event.event_id[:8] }}` **{{ event.start_time | format_time }}** — {{ event.topic }}{% if event.chat_content_tags %}（{{ event.chat_content_tags | join("、") }}）{% endif %}

{% endfor %}
{% else %}
{{ get_string("projector.no_events") }}
{% endif %}
