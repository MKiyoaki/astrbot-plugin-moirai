# {{ persona.primary_name }} 的印象记录

> 本文件可由用户手动编辑，修改将在下次同步时写回数据库（Phase 10）。

{% if impressions %}
{% for imp in impressions %}
---

## 来自 `{{ imp.observer_uid }}` 的印象（范围：{{ imp.scope }}）

| 字段 | 值 |
|------|----|
| 社交取向 | {{ imp.ipc_orientation }} |
| 亲和度 | {{ imp.benevolence | format_affect }} |
| 支配度 | {{ imp.power | format_affect }} |
| 情感强度 | {{ (imp.affect_intensity * 100) | int }}% |
| 拟合优度 | {{ (imp.r_squared * 100) | int }}% |
| 置信度 | {{ (imp.confidence * 100) | int }}% |
| 最近强化 | {{ imp.last_reinforced_at | format_time }} |

{% if imp.evidence_event_ids %}
**依据事件：** {{ imp.evidence_event_ids | join(", ") }}
{% endif %}
{% endfor %}
{% else %}
暂无印象记录
{% endif %}
