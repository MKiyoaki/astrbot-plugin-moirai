# ── Runtime ──────────────────────────────────────────────────────────────────
# Core storage
aiosqlite>=0.19
# Jinja2 templates for Markdown projector
jinja2>=3.1
# WebUI HTTP server; aiohttp is also a transitive dep of AstrBot
aiohttp>=3.9
# WebUI auth
bcrypt>=4.0
httpx>=0.27.0
# Vector storage extensions
sqlite-vec>=0.1.9
# Data validation
pydantic>=2.0

# ── Optional runtime (Manual install recommended for local ML features) ───────
# These are NOT in the default install to keep the plugin lightweight and fast.
# To enable local vector search, run: pip install sentence-transformers
# To enable semantic clustering extraction, run: pip install scikit-learn

# sentence_transformers>=3.0.0
# scikit-learn>=1.3.0

# ── Development / test ────────────────────────────────────────────────────────
pytest>=7.4
pytest-asyncio>=0.23
