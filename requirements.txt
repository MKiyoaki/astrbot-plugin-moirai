# Runtime dependencies (grow as phases progress)
# Phase 2+: aiosqlite>=0.19
# Phase 5+: faiss-cpu>=1.7; sentence-transformers>=2.6
# Phase 7+: jinja2>=3.1

# Development / test
pytest>=7.4
pytest-asyncio>=0.23
